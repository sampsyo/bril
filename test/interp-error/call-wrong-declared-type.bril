main {
    x : int = const 2;
    y : int = const 2;
    z : int = call add2(x, y);
    print y;
    print z;
}
bool add2(x : int, y : int) {
    w : int = add x y;
    y : int = const 5;
    print w;
    ret w;
}