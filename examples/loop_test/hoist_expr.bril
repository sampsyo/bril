# ARGS: live

main {
  result: int = const 1;
  i: int = const 10;

header:
  # Enter body if i >= 0.
  zero: int = const 0;
  one: int = const 1;
  two: int = add one one;
  cond: bool = gt i zero;
  br cond body end;

body:
  result: int = mul result i;
  i: int = sub i two;
  jmp header;

end:
  print result;
}
