main {
  size: int = const 8192;
  vecSize: int = const 4;

  a: int = const 0;
  b: int = add a size;
  c: int = add b size;

  i: int = const 0;
vvadd_loop:
  ai: int = add a i;
  bi: int = add b i;
  ci: int = add c i;

  va: vector = _svload ai;
  vb: vector = _svload bi;
  vc: vector = _svadd va vb;
  _svstore vc ci;

  i: int = add i vecSize;
  done: bool = ge i size;
  br done vvadd_done vvadd_loop;
vvadd_done:

}
