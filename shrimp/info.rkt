#lang info

(define version "0.1")

(define deps (list "threading"
                   "rosette"
                   "graph"))

(define build-deps (list "threading"
                         "rosette"
                         "graph"))
